#include <cstdio>

#include <iostream>
#include <string>
#include <vector>
#include <set>

#include <algorithm> // sort
#include <cmath> // floor

using namespace std;

long long N;

int main() {
  while(1) {
    scanf("%lld", &N); // 1 <= N <= 100,000
    if(N == 0) break;

    long long* input = new long long[N]; // 1 <= N <= 10 * 10^8
    for(int i = 0; i < N; i++) scanf("%lld", &input[i]);


  }
}